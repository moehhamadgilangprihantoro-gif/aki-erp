import { MapPin, Plus } from 'lucide-react'
import { requireCustomer } from '@/lib/auth'
import { addAddress } from '../actions'

type Props={searchParams:Promise<{error?:string;success?:string}>}
export default async function AccountAddressesPage({searchParams}:Props){const params=await searchParams;const{supabase,userId}=await requireCustomer();const{data:customer}=await supabase.from('customers').select('id').eq('user_id',userId).single();const{data:addresses}=customer?await supabase.from('customer_addresses').select('*').eq('customer_id',customer.id).order('is_default',{ascending:false}):{data:[]};return <><div className="account-title"><div><h1>Alamat Saya</h1><p>Alamat pengiriman dan pemasangan.</p></div></div>{params.error&&<div className="account-alert error">{params.error}</div>}{params.success&&<div className="account-alert success">{params.success}</div>}<div className="account-two-column"><section className="account-panel"><div className="account-panel-title"><h2>Alamat Tersimpan</h2></div><div className="address-list">{(addresses??[]).map(a=><article key={a.id}><span><MapPin/></span><div><div><strong>{a.label}</strong>{a.is_default&&<em>Utama</em>}</div><h3>{a.recipient_name}</h3><p>{a.phone}</p><p>{a.address_line}, {a.city} {a.postal_code||''}</p></div><button>Ubah</button></article>)}{!addresses?.length&&<div className="account-empty"><MapPin/><h3>Belum ada alamat</h3><p>Tambahkan alamat untuk pengiriman dan pemasangan.</p></div>}</div></section><aside className="account-panel account-form-panel"><div className="account-panel-title"><h2><Plus/> Tambah Alamat</h2></div><form action={addAddress} className="account-form"><label>Label Alamat<input name="label" placeholder="Rumah / Kantor"/></label><label>Nama Penerima<input name="recipientName" required/></label><label>Nomor Telepon<input name="phone" required/></label><label>Alamat Lengkap<textarea name="addressLine" required/></label><div><label>Kota<input name="city" required/></label><label>Kode Pos<input name="postalCode"/></label></div><label className="account-checkbox"><input name="isDefault" type="checkbox"/> Jadikan alamat utama</label><button>Simpan Alamat</button></form></aside></div></>}
